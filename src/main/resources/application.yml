---
micronaut:
    application:
        name: preparedq

---
postgres:
    reactive:
        client:
        port: 5432
        host: localhost
        database: preparedq
        # user: user
        # password: password
        # maxSize: 5